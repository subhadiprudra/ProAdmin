<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easylife.proadmin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DeleteExpairedMatch"></activity>
        <activity android:name=".JoinendPerson2" />
        <activity android:name=".JoinedPerson" />
        <activity android:name=".SendNotification" />
        <activity android:name=".Block" />
        <activity android:name=".Accounts" />
        <activity android:name=".MoneyRequest" />
        <activity android:name=".Result0" />
        <activity android:name=".ResultView" />
        <activity android:name=".Result" />
        <activity android:name=".ReleaseIdPass" />
        <activity android:name=".Matches" />
        <activity android:name=".CreateMatch" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>